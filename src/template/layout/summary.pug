include ../config
block config
include ../mixins/mixins

doctype
html(lang= lang)
  head
    include ../partials/head
    link(rel='stylesheet', href= path + 'assets/styleguide.css')

  body(class= template)
    block content

    script(src= path + "assets/app.js", async)

